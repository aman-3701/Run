@Library('shared') _

pipeline {
    agent any
    stages {
        stage('test') {
            steps {
                script {
                    clone()
                }
            }
        }
    }
}
